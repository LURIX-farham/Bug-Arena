# =====================================================================
# Bug Arena — production environment template
# Copy to:  public_html/.env      (⚠ the ROOT of public_html, NOT api/)
# Then replace every <...> placeholder with your real host values.
# Why here? backend/config/config.php looks for .env two folders above
# its own location, which resolves to public_html on the standard
# layout. The root .htaccess denies all dotfiles, so this file can
# never be downloaded over HTTP.
# =====================================================================

# Production flag: turns OFF debug output, auto DB setup and setup.php
BUGARENA_ENV=production
BUGARENA_DEBUG=0
BUGARENA_AUTO_SETUP=0

# --- MySQL (create these in cPanel > MySQL Databases) ---
# cPanel prefixes user/db names with your account name, e.g. mysite_arena
BUGARENA_DB_HOST=localhost
BUGARENA_DB_PORT=3306
BUGARENA_DB_NAME=<CPANEL_USER>_bug_arena
BUGARENA_DB_USER=<CPANEL_USER>_bugarena
BUGARENA_DB_PASSWORD=<رمز-قوی-تولید-شده-توسط-cPanel>

# --- CORS: exact origin of the deployed frontend (no trailing slash) ---
BUGARENA_CORS_ORIGINS=https://<your-domain.ir>

# --- Session lifetime in seconds (2h of inactivity) ---
BUGARENA_SESSION_LIFETIME=7200
