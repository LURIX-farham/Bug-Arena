# Bug Arena v1.9.0 — تکمیل گیم مود 1v1 (Duel)

این نسخه، کار نیمه‌تمام گیم مود دوم سایت (دوئل ۱v۱ انلاین) را کامل می‌کند. بک‌اند و بخشی از فرانت‌اند از قبل آماده بود؛ در این نسخه حلقه‌های باقی‌مانده بسته شدند و حالا کل جریان دوئل از Start تا اعلام برنده به‌صورت کامل و سرتاسری کار می‌کند.

---

## جریان کامل بازی (همان چیزی که درخواست شده بود)

1. کاربر وارد صفحه `/duel` می‌شود، سختی (Easy / Medium / Hard / هر چی) و نوع باگ را قبل از استارت شخصی‌سازی می‌کند و روی **START DUEL** می‌زند.
2. سرور یک match می‌سازد و درخواست دوئل را برای **همه‌ی کاربران آنلاین** ارسال می‌کند؛ یک toast (مسیج) بالای صفحه‌ی آن‌ها ظاهر می‌شود — روی هر صفحه‌ای که باشند، نه فقط لابی.
3. اولین کاربری که روی **OK (قبول)** بزند صندلی را برمی‌دارد (پذیرش اتمیک و مقاوم به رقابت؛ بقیه پیام «کسی دیگر زودتر قبول کرد» یا «منقضی شد» می‌بینند).
4. هر دو طرف وارد صفحه‌ی رو به رویی (`/duel/:matchId`) می‌شوند: پروفایل و نام کاربری هر دو بازیکن روبه‌روی هم با فاصله و انیمیشن **VS** نمایش داده می‌شود.
5. سپس انیمیشن «کامپیوتر در حال انتخاب باگ» (اسلات‌ماشین) اجرا می‌شود و یک **bug challenge رندوم** مطابق فیلترهای میزبان قفل می‌شود.
6. هر دو بازیکن به همان ادیتور کد گیم مود solo وارد می‌شوند (`/arena/:id?mode=duel&match=...`) و با هم مسابقه می‌دهند.
7. وقتی کار هر دو تمام شد (یا زمان تمام شود)، سرور بر اساس سرعت، دقت دیباگ و مقیاس‌های دیگر نتیجه را محاسبه و برنده را اعلام می‌کند:
   - **برنده: ۹۰٪ امتیاز باگ + ۲۰٪ جایزه**
   - **بازنده: ۱۰٪ امتیاز باگ**
   - مساوی: هر دو ۱۰٪
   - به‌همراه ریتینگ Elo (K=32) و آپدیت برد/باخت و آمار.
8. در آرنا، اورلی نتیجه نمایش داده می‌شود: پیروزی / شکست / مساوی + امتیاز کسب‌شده + **جدول مقایسه نهایی** دو طرف (امتیاز کل، پاداش سرعت، زمان حل، تعداد تلاش، هاردنینگ).

---

## چیزی که در این نسخه تکمیل شد

### فرانت‌اند
| فایل | تغییر |
|------|-------|
| `src/App.jsx` | روت‌های `/duel` (لابی) و `/duel/:matchId` (اتاق مسابقه) اضافه شدند. |
| `src/components/layout/AppShell/AppShell.jsx` | کامپوننت سراسری `DuelInvitationListener` (toast دعوت‌نامه) روی همه صفحات احراز هویت‌شده mount شد. |
| `src/components/duel/DuelInvitationListener.jsx` | **heartbeat سراسری** (هر ~۲۰ ثانیه از هر صفحه‌ای) اضافه شد تا کاربر همیشه در لیست آنلاین‌ها دیده شود و دعوت‌نامه‌ها به او برسند؛ toast در حین دوئل فعال نمایش داده نمی‌شود. |
| `src/pages/Arena/Arena.jsx` | هسته‌ی تکمیل دوئل: polling وضعیت مسابقه، ثبت نتیجه در سرور هنگام Submit، **ثبت خودکار نتیجه‌ی ناتمام هنگام اتمام زمان** (تا حریف منتظر نماند)، کارت «در انتظار حریف»، اورلی حکم نهایی (پیروزی/شکست/مساوی + جدول مقایسه + امتیاز)، قفل شدن ادیتور اگر نتیجه قبلاً ثبت شده باشد (ریلود وسط دوئل)، مسیر خروج به لابی دوئل. |
| `src/pages/Arena/Arena.css` | استایل‌های کارت انتظار، جدول مقایسه، اورلی حکم (win/lose/draw) با همان دیزاین‌سیستم و پشتیبانی RTL. |
| `src/components/layout/AppShell/AppSidebar.jsx` + `AppMobileNav.jsx` | آیتم ناوبری «Duel / دوئل» (از قبل ترجمه‌اش آماده بود) به سایدبار و منوی موبایل اضافه شد. |
| `src/pages/Landing/sections/GameModes.jsx` | تب 1v1 در لندینگ حالا به `/duel` لینک می‌شود. |
| `src/pages/Home/Home.jsx` + `Home.css` | دکمه‌ی «شروع دوئل ۱v۱» در داشبورد خانه. |
| `src/i18n/translations/duel.js` و `core.js` | کلیدهای جدید EN/FA: `you`, `rival`, `opponentDone`, `backToHome`, `home.duelCta`. |

### بک‌اند
| فایل | تغییر |
|------|-------|
| `backend/src/Controllers/DuelController.php` | جلوگیری از اسپویل امتیاز حریف: تا وقتی دوئل فعال است، نتیجه‌ی خام حریف به کلاینت ارسال نمی‌شود (فقط فلگ «تمام کرد/نکرد»). فیلد `progress` برای نمایش زنده‌ی وضعیت اضافه شد. |

### اصلاح منطقی مهم
- نتیجه‌ی دوئل دیگر به‌عنوان «بهترین رکورد شخصی سولو» ثبت نمی‌شود؛ یعنی یک اجرا نمی‌تواند هم XP سولو بگیرد و هم امتیاز دوئل (جلوگیری از اعتبار مضاعف). امتیاز دوئل فقط از مسیر مسابقه پرداخت می‌شود.

### نسخه‌سازی
- `package.json` → `1.9.0` و به‌روزرسانی مسیر `BugArena-v1.9.0` در `.env.example`، فال‌بک `apiClient.js`، `setup.php` و READMEها.
- پوشه‌ی `dist/` با کد جدید build شده است (برای استقرار بدون npm).

---

## چطور تست کنم؟ (دو مرورگر)

1. پوشه را در `C:\wamp64\www\BugArena-v1.9.0` قرار دهید و WAMP را روشن کنید.
2. `http://localhost/BugArena-v1.9.0/backend/public/health` را باز کنید (دفعه‌ی اول دیتابیس را خودش می‌سازد؛ جدول‌های دوئل هم در schema.sql هست و برای دیتابیس‌های قدیمی مایگریشن `003_duel_mode.sql` وجود دارد).
3. فرانت: `npm install && npm run dev` یا استفاده از پوشه‌ی آماده‌ی `dist/`.
4. دو جلسه‌ی مرورگر بسازید (یکی normal، یکی incognito) و با دو حساب مختلف ثبت‌نام/لاگین کنید.
5. بازیکن A: `/duel` → انتخاب فیلترها → START DUEL.
6. بازیکن B: روی هر صفحه‌ای که باشد، toast دوئل را بالای صفحه می‌بیند → OK.
7. هر دو: مراسم VS + انتخاب چالش → ادیتور کد → بعد از Submit هر دو نفر (یا اتمام زمان)، حکم نهایی و جدول مقایسه نمایش داده می‌شود.

> اگر نام پوشه را تغییر می‌دهید، `VITE_API_BASE_URL` در `.env` را هماهنگ کنید و `npm run build` را دوباره اجرا کنید.

## نکته‌های فنی
- کاملاً مبتنی بر polling (بدون وب‌سوکت): heartbeat هر ~۲۰ ثانیه، inbox هر ~۴ ثانیه، وضعیت مسابقه هر ~۲.۵ ثانیه.
- دعوت‌نامه‌ها بعد از ۹۰ ثانیه منقضی می‌شوند؛ مسابقات رهاشده به‌صورت lazy پاک/تسویه می‌شوند.
- سرور مالک اصلی امتیاز و ریتینگ است و نتیجه‌ی هر اجرا را سمت سرور بازمحاسبه می‌کند.
